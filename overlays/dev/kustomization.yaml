apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Automatically prefixes all resources (e.g., dev-web-app)
namePrefix: dev-

# Adds environment labels to all resources
commonLabels:
  environment: development
